# Copy to load_tests/.env and fill in real values (load_tests/.env is gitignored).

# Target server. Point at the TEST SERVER — never production.

LOAD_HOST=

# A seeded, already-verified alumni account on the target environment.
LOAD_EMAIL=
LOAD_PASSWORD=
