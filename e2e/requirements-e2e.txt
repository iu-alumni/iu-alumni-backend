# E2E (browser) test dependencies — separate from requirements-test.txt,
# which covers the backend's own unit/integration suite under tests/.
playwright>=1.47,<2
pytest>=8.3,<9
pytest-playwright>=0.5.1,<0.6
pytest-rerunfailures>=14.0,<15
pytest-html>=4.1,<5
pydantic-settings>=2.5,<3
Faker>=30.0,<31
