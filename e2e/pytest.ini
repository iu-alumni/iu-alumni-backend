[pytest]
testpaths = tests
addopts = -ra --strict-markers --tb=short
markers =
    mobile: scenarios against the mobile web app (Flutter Web, CanvasKit)
    admin: scenarios against the admin dashboard (Nuxt)
    negative: negative / validation scenarios
    smoke: critical-path happy-path scenarios
